function myFunction() {
    let title = document.getElementById("title").value;
    let lname = document.getElementById("lname").value;
    alert(`Thanks ${title} ${lname}, your registration is done!`);
  }

  //Maria Magdalena WICHROWSKA
  //the file was changed to .txt because otherwise I couldn't send it by email